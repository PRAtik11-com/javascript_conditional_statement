let a = 12 
let b = 45
let c = 85 

if (a >= b && a >= c ) {
     console.log(`${a} is maximum number.`);
} else if(b >= a && b >= c){
    console.log(`${b} is maximum number.`);
}else {
    console.log(`${c} is maximum number.`);
}